using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class OutsideShop : MonoBehaviour
{
    /*public int[] upgradesOwned = new int[3];

    public Button[] upgradeButtons;

    public string playerPref;


    // Start is called before the first frame update
    void Start()
    {
        for (int i = 0; i < 3; i++)
        {
            playerPref = string.Format("", i);
            upgradesOwned[i] = PlayerPrefs.GetInt(playerPref, 0);
            for (int j = 0; j < 5; j++)
            {
                if (upgradesOwned[i] * (i + 1) > j)
                    upgradeButtons[i].interactable = false;
            }
            
            
            
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void settingArray(int[] arr, Button[] btnArr)
    {

    }*/

}
